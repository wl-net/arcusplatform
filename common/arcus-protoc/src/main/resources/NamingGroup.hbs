////////////////////////////////////////////////////////////////////////////////
// This file has been generated by the Iris Protocol Compiler. To change
// this file please change the underlying IRP file.
////////////////////////////////////////////////////////////////////////////////

package {{package}};

import com.arcussmarthome.protoc.runtime.ProtocStruct;
import com.arcussmarthome.protoc.runtime.ProtocNamingGroups;
import com.arcussmarthome.protoc.runtime.ProtocNamingMessages;

import java.util.Collections;
import java.util.HashMap;
import java.util.Map;

public enum Zigbee{{capitalize subpackage}}Naming implements ProtocNamingGroups {
   INSTANCE;

   private final Map<String,ProtocNamingMessages> GROUPS;

   private Zigbee{{capitalize subpackage}}Naming() {
      Map<String,ProtocNamingMessages> names = new HashMap<>();
      {{#each bindings}}
      names.put("{{toLowerCase this}}", {{this}}Naming.INSTANCE);
      {{/each}}

      GROUPS = Collections.unmodifiableMap(names);
   }

   @Override
   public Map<String,ProtocNamingMessages> getGroups() {
      return GROUPS;
   }

   @Override
   public ProtocStruct wrap(ProtocStruct msg, int profile, int endpoint, int cluster, int command, int manuf, boolean isClusterSpecific, boolean isFromServer, boolean isDisableDefaultResponse) {
      return com.arcussmarthome.driver.groovy.zigbee.ZigbeeNaming.INSTANCE.wrap{{capitalize subpackage}}Message(msg, profile, endpoint, cluster, command, manuf, isClusterSpecific, isFromServer, isDisableDefaultResponse);
   }

   @Override
   public int getDefaultEndpoint() {
      return com.arcussmarthome.driver.groovy.zigbee.ZigbeeNaming.INSTANCE.getDefault{{capitalize subpackage}}Endpoint();
   }

   @Override
   public int getDefaultProfile() {
      return com.arcussmarthome.driver.groovy.zigbee.ZigbeeNaming.INSTANCE.getDefault{{capitalize subpackage}}Profile();
   }
}
