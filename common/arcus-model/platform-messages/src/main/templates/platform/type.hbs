   public static final com.arcussmarthome.capability.definition.AttributeType TYPE_{{toUpperCase name}} =
   {{#if type.enum}}
      com.arcussmarthome.capability.definition.AttributeTypes.enumOf(
         java.util.Arrays.<String>asList({{#enumValues}}"{{.}}"{{#unless @last}}, {{/unless}}{{/enumValues}})
      );
   {{else}}
      com.arcussmarthome.capability.definition.AttributeTypes.parse("{{{type.representation}}}");
   {{/if}}

   public static final com.arcussmarthome.device.attributes.AttributeKey<{{{typeRepresentationToJavaType type.representation}}}> KEY_{{toUpperCase name}} =
    {{#if type.enum}}
      com.arcussmarthome.device.attributes.AttributeKey.create(ATTR_{{toUpperCase name}}, String.class);
   {{else}}
      (com.arcussmarthome.device.attributes.AttributeKey<{{{typeRepresentationToJavaType type.representation}}}>) com.arcussmarthome.device.attributes.AttributeKey.createType(ATTR_{{toUpperCase name}}, com.arcussmarthome.capability.definition.AttributeTypes.parse("{{{type.representation}}}").getJavaType());
   {{/if}}